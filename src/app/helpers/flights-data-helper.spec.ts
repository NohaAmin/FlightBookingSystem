import { FlightsDataHelper } from './flights-data-helper';

describe('FlightsDataHelper', () => {
  it('should create an instance', () => {
    expect(new FlightsDataHelper()).toBeTruthy();
  });
});
